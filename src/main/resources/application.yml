spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/project_diogo?useTimezone=true&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password:
    initialization-mode: always
    platform: mysql

  jpa:
    generate-ddl: false
    hibernate.dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate.ddl-auto: none
    hibernate:
      jdbc: time_zone = TimeZone